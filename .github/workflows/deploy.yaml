name: Deploy to GitHub Pages

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

# Permissions for the official GitHub Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"   # eunoia requires Python >=3.11
      - uses: quarto-dev/quarto-actions/setup@v2
      - name: Install VISET + docs deps
        run: |
          python -m pip install --upgrade pip
          # see the matching comment in test.yaml: upsetplot's PyPI release is broken
          # under numpy>=2.4, so install the unmerged upstream fix (jnothman/UpSetPlot#302)
          pip install "upsetplot @ git+https://github.com/durr1602/UpSetPlot.git@7fa2661b7989e733bfce8a668f864084b8390afd"
          pip install -e ".[dev]" "pandas<3"
      - name: Build docs
        run: nbdev-docs
      - uses: actions/configure-pages@v5
      - name: Upload docs artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
